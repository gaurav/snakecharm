[versions]
# libraries
junit = "5.13.4"
cucumber = "7.19.0"
# kotlinx-serialization, for SmkWrapperStorage's CBOR round-trip. Kept at the version the target
# IntelliJ Platform bundles (see kotlinxSerializationPlatform below) so we neither compile against an
# older API than we run on, nor ship a copy that shadows the platform's on the flat test classpath.
#
# WHEN BUMPING `platformVersion`: re-check this against the new platform and update both this and
# kotlinxSerializationPlatform. AGENTS.md -> "A platform bump moves more than platformVersion" lists
# how, and issue #587 has the full write-up of what goes wrong when they drift apart.
cbor = "1.9.0"
opentest4j = "1.3.0"

# plugins
kotlin = "2.2.0"
# Kotlin standard library bundled with the target IntelliJ Platform (PyCharm 2026.1.3 / build 261).
# The runtime kotlin-stdlib is forced to this version so it can read the v2 @DebugMetadata emitted by
# the platform's Kotlin-compiled classes (see the resolutionStrategy in build.gradle.kts). Keep in sync
# with the `platformVersion` in gradle.properties.
kotlinPlatform = "2.3.20"
# kotlinx-serialization bundled with the target platform. Forced onto the runtime classpaths for the
# same ABI reason as kotlinPlatform -- see the resolutionStrategy in build.gradle.kts for what breaks
# without it, and issue #587 for the full write-up. Keep in sync with `platformVersion` in
# gradle.properties; AGENTS.md -> "A platform bump moves more than platformVersion" is the checklist.
#
# To find the value for a new platform, read the manifest of the jar the platform ships:
#   unzip -p <ide>/lib/intellij.libraries.kotlinx.serialization.core.jar META-INF/MANIFEST.MF \
#     | grep Implementation-Version
# On 2026.1.3 and on 2026.2.1 that reports 1.9.0.
kotlinxSerializationPlatform = "1.9.0"
intelliJPlatform = "2.16.0" # https://github.com/JetBrains/intellij-platform-gradle-plugin
changelog = "2.4.0"
kover = "0.9.1"
qodana = "2025.1.1"

[libraries]
junit = { group = "org.junit", name = "junit-bom", version.ref = "junit" }
cucumber = { group = "io.cucumber", name = "cucumber-bom", version.ref = "cucumber" }
kotlinTestJunit = { group = "org.jetbrains.kotlin", name = "kotlin-test-junit", version.ref = "kotlin" }
kotlinReflect = { group = "org.jetbrains.kotlin", name = "kotlin-reflect", version.ref = "kotlin" }
kotlinStdlibJdk8 = { group = "org.jetbrains.kotlin", name = "kotlin-stdlib-jdk8", version.ref = "kotlin" }
kotlinxCbor = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-cbor", version.ref = "cbor" }
opentest4j = { group = "org.opentest4j", name = "opentest4j", version.ref = "opentest4j" }


[plugins]
kotlin = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
intelliJPlatform = { id = "org.jetbrains.intellij.platform", version.ref = "intelliJPlatform" }
changelog = { id = "org.jetbrains.changelog", version.ref = "changelog" }
kover = { id = "org.jetbrains.kotlinx.kover", version.ref = "kover" }
qodana = { id = "org.jetbrains.qodana", version.ref = "qodana" }